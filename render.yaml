services:
  - type: web
    name: sistema-helpdesk
    runtime: python
    buildCommand: pip install -r requirements.txt && python seed.py
    startCommand: gunicorn run:app
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: "3.12"
      - key: SECRET_KEY
        generateValue: true
